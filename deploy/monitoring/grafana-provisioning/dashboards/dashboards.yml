apiVersion: 1

providers:
  - name: "Docstore"
    orgId: 1
    folder: "Docstore"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
